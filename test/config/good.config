runas shiny;
max_workers 500;

server {
  listen 80;
  server_name localhost;

  location /users {
    user_apps on;
  }

  location / {
    #runas nobody;
    #app_dir /var/shiny-server/www;
    redirect http://www.rstudio.com/;
  }
}
